<h1> Recent Games </h1>

<% @recent_games.each do |game|%>
        <%#<%(game.game_name, game_path(game.id)) %>
        <%=link_to(game.game_name, game_path(game.id))%>
        <%= game.created_at.to_date%>
                <% end %>