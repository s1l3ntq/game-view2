
<h1>GameFeed</h1>
<h2>
    <% @games.each do |game|%>
        <%= link_to(game.game_name, game_path(game.id) ) %>
            <%= game.demo%>
                <% end %>
</h2>

<h3>
    <%= link_to "Create Game Post" , new_game_path(@game), method: 'create' %>
</h3>