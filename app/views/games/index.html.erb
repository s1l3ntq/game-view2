<%= link_to "Recent Games", (recent_games_games_path)%> <%= link_to "Create Game Post" , new_game_path(@game), method: 'create' %> <%= link_to "My Games", user_path(@current_user) %> <%= link_to "Game Feed", games_path %>

<h2>GameFeed</h2>
<h3>
    <% @games.each do |game|%>
        <%= link_to(game.game_name, game_path(game.id) ) %>
            <%= game.demo%>
                <% end %>
</h3>



<p><%= link_to "Logout", (logout_path)%>
</p>