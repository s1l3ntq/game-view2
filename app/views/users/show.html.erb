<h1><%=current_user.username%></h1>

<h2> My Games</h2>
<%#<%byebug%>
 <% @current_user.games.each do |game|%>
        <%= link_to(game.name, game_path(game.id)) %>
            <%#<%= game.demo%>
                <% end %>

<%#<%=@current_user.games.all%>

